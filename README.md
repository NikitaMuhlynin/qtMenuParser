This is a training program I wrote to learn the Qt framework. It implements parsing of JSON data used to build a menu, but without using built-in parsing tools, using streams and overloaded operators. 
It's easy to extend and implement in your projects to build a user-friendly UI that can be easily defined using JSON.
